services:
  warp:
    build: .
    ports:
    - 1088:1080
    restart: unless-stopped
    environment:
    - "WARP_LICENSE="
    - "FAMILIES_MODE=off"
    volumes:
    - ./warp:/var/lib/cloudflare-warp
